node {
    checkout scm
 
    docker.withRegistry('https://us.gcr.io', 'gcr:personal-1332') {

        stage('Create Docker Image') {
            dir('404page') {
                def Img = docker.build("personal-1332/404page:${env.BUILD_NUMBER}")
                Img.push()
                Img.push("latest")
            }
        }
    }
}
